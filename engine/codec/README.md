<!-- Docstring: Codec module overview for v0.1 CPU-first engine. -->
# Codec Module (v0.1)

This directory documents the discrete codec layer that will translate audio
into token streams and back. The v0.1 milestone only defines structure and
interfaces, reserving implementation and model bindings for future iterations.
